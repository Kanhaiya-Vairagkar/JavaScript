
function name1() {
    console.log("Kanhaiya Vairagkar");
    for (let a = 0; a < 5; a++) {
        console.log(" Kanhaiya " + a);
    }
}
name1();

function print() {
    console.log("alerts for the database and server conditions configured in your alert settings. When a condition triggers an alert, Atlas displays a warning symbol on the cluster and sends alert notifications.");
    var a = 90;
    return a;
}
var c = print();
console.log(c);

function prints() {
    console.log(" issues alerts for the database and server conditions configured in your alert settings. When a condition triggers an alert, Atlas displays a warning symbol on the cluster and sends alert notifications.");
    return "ABC";
}
var a = prints();
console.log(a);